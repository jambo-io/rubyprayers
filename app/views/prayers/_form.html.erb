<%= form_with(model: prayer, local: true) do |form| %>
  <% if prayer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prayer.errors.count, "error") %> prohibited this prayer from being saved:</h2>

      <ul>
      <% prayer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :prayer %>
    <%= form.text_area :prayer %>
  </div>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author %>
  </div>

  <%= form.collection_check_boxes :category_ids, Category.all, :id, :name %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
